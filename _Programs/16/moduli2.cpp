#include <iostream>
#include "mymath.h"
using namespace std;

int main()
{
    int x=30, y=2;
    cout << Reiz(x,y) << endl;
    cout << Dal(x,y) << endl;
    cout << Sum(x,y) << endl;
    cout << Atn(x,y) << endl;
    system("pause");
    return 0;
}
